# Daily Log: 2026-02-20

## Summary
- Upgraded Polymarket Bot to "Phase 2: Maker-Ready".
- Fixed BinanceBot scanning issue (0 signals for days).

## Polymarket Bot Update (Phase 2)
- Upgraded from "Scanner" to "Maker-Ready" architecture.
- **New Modules:**
    - `maker/fees.py`: Fee manager (mocked, pluggable).
    - `maker/risk.py`: Inventory & exposure logic (global limits).
    - `maker/quoter.py`: Quote instruction generator (strict JSON schema).
    - `maker/paper_executor.py`: Paper execution stub.
- **Current Status:** Running continuously (PID 168200).
- **Safety:** Size capped at $10/order due to missing orderbook data ("unknown" liquidity).
- **Next Step:** Phase 3 (Market-Aware Paper Trading).

## BinanceBot Fix
- **Issue:** Scanner produced 0 signals for days.
- **Root Cause:**
    1. `klines` limit was 200, but strategy required >210 candles.
    2. Missing technical indicators in DataFrame (pandas-ta missing).
- **Fix:**
    1. Bumped `klines` limit to 300.
    2. Implemented manual indicator calculation (`bot/services/indicators.py`).
    3. Updated `run_agent.py` to inject indicators before strategy call.
- **Result:** Bot is now finding candidates (e.g., ALLOUSDT, KITEUSDT).
